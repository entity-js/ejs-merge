# EntityJS - Utilities

## merge

A small utility which merges objects or arrays.

### Usage

```javascript
var merge = require('merge');

var obj1 = {
      value: 1, hello: 'world'
    },
    obj2: {
      foo: 'bar'
    };

merge(obj1, obj2);
```
